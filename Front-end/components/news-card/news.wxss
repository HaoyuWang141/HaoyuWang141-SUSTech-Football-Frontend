/* components/news-card/news.wxss */
.news-card {
  margin: 10rpx;
  background-color: #fff;
  border-radius: 10rpx;
  box-shadow: 0 2rpx 10rpx rgba(0, 0, 0, 0.4);
  padding: 20rpx 20rpx;
}

.image {
  display: block;
  flex-shrink: 0;
  max-width: 100%;
}

.text-box {
  margin: 20rpx 20rpx;
  display: -webkit-box;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2; /* 限制在两行 */
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: normal; /* 允许换/行 */
  line-height: 1.5; /* 根据需要调整行高，以改善可读性 */
  flex-grow: 1;
}
